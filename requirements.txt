# requirements.txt
# الأساسيات
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
sqlalchemy==2.0.23

# الشبكات
netmiko==4.2.0
paramiko==3.4.0
ncclient>=0.7.0
pysnmp==4.4.12

# الاتصالات
requests==2.31.0
aiohttp==3.9.1
websockets==12.0
zeep==4.2.1

# المراقبة
# opencv-python==4.8.1  # Heavy, removed for Vercel
onvif-zeep==0.2.12
aiortsp==1.3.0

# المنازل الذكية
zigpy==0.57.0
paho-mqtt==1.6.1
xknx==2.9.0

# الأنظمة الصناعية
pymodbus==3.6.2
asyncua==1.0.0
bacpypes==0.18.6

# المراقبة
prometheus-client==0.19.0
grafana-api==1.0.3

# الأمان
cryptography==41.0.7
pyjwt==2.8.0
bcrypt==4.1.2
python-keycloak>=3.3.0

# الذكاء الاصطناعي والتحليل (اختياري - يتم تفعيل وضع المحاكاة في حالة عدم التثبيت)
# Removed for Vercel Serverless (Size Limits)
# torch>=2.1.0
# transformers>=4.35.0
# scikit-learn>=1.3.2
# xgboost>=2.0.2
# scipy>=1.11.3
# pandas>=2.1.3
# hmmlearn>=0.3.0
# tensorflow>=2.14.0

# التحليل الجنائي وتتبع التهديدات (اختياري - يتم تفعيل وضع المحاكاة في حالة عدم التثبيت)
# volatility3>=2.5.0
# rekall-core>=1.7.2
# pytsk3>=20210419
# pyewf>=20201230
# yara-python>=4.3.1
# capstone>=5.0.1
networkx>=3.1
elasticsearch>=8.11.0

# الإجراءات المضادة (اختياري - يتم تفعيل وضع المحاكاة في حالة عدم التثبيت)
scapy>=2.5.0
# python-iptables>=1.0.0 # Requires system level access
# NetfilterQueue>=1.1.0 # Requires system level access

# مراقبة السحابة (اختياري - يتم تفعيل وضع المحاكاة في حالة عدم التثبيت)
boto3>=1.28.0
google-cloud-monitoring>=2.15.0
azure-mgmt-monitor>=5.0.0
azure-identity>=1.14.0
